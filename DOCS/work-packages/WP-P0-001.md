# WP-P0-001 - Governance and environment bootstrap

## Authority and purpose
Establish and validate the OrbitMark autonomous governance and delivery layer so that
every later phase runs under enforceable controls. Authority: `CLAUDE.md`,
`DOCS/00_README_AND_AUTHORITY.md`, `DOCS/02_AUTONOMOUS_DEVELOPMENT_MASTER_PLAN.md`
(§6 P0, §7 gate logic), `DOCS/03_PROJECT_SKILLS_STRATEGY_AND_REGISTRY.md` (§3
mandatory generated registry), `DOCS/04_COUNCIL_CHARTER_AND_REVIEW_PROTOCOL.md`,
`DOCS/07_RELEASE_GATES_AND_DEFINITION_OF_DONE.md` (Gate G-P0). This package does not
implement product features and does not scaffold the product monorepo (that is
WP-P1-001).

## Controlling requirements / ADRs / screens
Governance/process requirements for this package (no product FRs at P0; product
requirement traceability begins at WP-P1-001 when product code exists):
- **GOV-P0-1** Repository governance structure exists (DOCS, `.claude/{skills,agents,rules}`, scripts, CI, evidence, templates).
- **GOV-P0-2** `.claude/settings.json` parses; permissions and hooks are valid and reference existing scripts.
- **GOV-P0-3** All 15 mandatory skills and 12 council agents exist and are well-formed.
- **GOV-P0-4** `DOCS/PROJECT_SKILLS.md` is generated from disk and matches 1:1 (no orphan skill, no orphan entry).
- **GOV-P0-5** `RUN_STATE.yaml` and `WORK_QUEUE.yaml` parse and model the phase graph.
- **GOV-P0-6** CI skeleton (`docs-and-guards`) runs the doc/registry/guard checks.
- **GOV-P0-7** Destructive/protected commands are denied by the guard hook (self-test).
Screens/states: none (P0 has no UI). ADRs: ADR-0001 (adopt governance package as-is).

## Dependencies and entry gate
Dependencies: none (root of the dependency graph). Entry gate: repository present and
controlled DOCS delivered. Status before this WP: `RUN_STATE.yaml` phase P0, status
READY, next_action "Run orbitmark-bootstrap".

## File ownership and branch/worktree
Branch: `wp/p0-001-governance-bootstrap` (one writer). Owned paths (added/validated):
`CLAUDE.md`, `DOCS/**`, `.claude/**`, `scripts/**`, `.github/workflows/ci.yml`,
`.github/workflows/claude-safe-work-package.yml`, `RUN_STATE.yaml`, `WORK_QUEUE.yaml`,
`PACKAGE_MANIFEST.json`, `artifacts/evidence/P0/**`, `DOCS/work-packages/WP-P0-001.md`.
Not owned / untouched: `backend/**`, `web/**`, `.github/workflows/deploy.yml` (legacy M0
spike and its Railway deploy; superseded by the greenfield monorepo at P1, not deleted).

## Assumptions and open items
- `pnpm` is not yet installed on the host; required only from WP-P1-001. Recorded as an
  open item, not a P0 blocker (no root `package.json` exists, so CI web/python jobs are
  inert at P0).
- The CDR `.docx/.pdf` are binary; product FR-level traceability is bound at P1 when the
  requirements register is machine-readable in-repo.
- Host is Windows/MINGW64; framework scripts run under Git Bash.

## Implementation slices
1. Run `scripts/bootstrap_project.sh`; capture environment report + evidence (DONE).
2. Validate settings JSON, hooks, agents, skills, scripts, CI YAML, state, queue (DONE).
3. Regenerate `DOCS/PROJECT_SKILLS.md`; confirm 1:1 coverage (DONE).
4. Author this work package + P0 requirements traceability + ADR-0001.
5. Commit the untracked governance layer on the feature branch.

## Test matrix (positive, negative, failure, performance)
- Positive: `scripts/validate-docs.py` PASS; `scripts/generate-skills-registry.py` writes
  registry with `git diff --exit-code` clean; `scripts/quality-gate.sh fast` PASS.
- Negative/failure (security control): `scripts/guard-destructive-selftest.sh` MUST block
  `git push origin main`, `rm -rf /`, `railway delete service`, and allow `git status`,
  `pnpm test`.
- Contract: `python -m json.tool .claude/settings.json` valid.
- Performance: N/A (no runtime product at P0).

## Screenshot/evidence matrix
No UI screenshots at P0 (no application). Evidence = environment report, validate-docs
log, skills-generation log, guard self-test log, quality-gate log, SHA256SUMS,
`manifest.json`, `COUNCIL_REVIEW.md`, `council-findings.json`, gate record.

## Council quorum
Planning quorum: Program Director (coordinator) + DevOps & Release Manager (owning
domain) + QA & Reliability Lead + Red-Team Adversary. Independent review quorum (phase
gate): Program Director + QA + DevOps/Release + Security & Privacy + Red-Team.

## Acceptance criteria
All of GOV-P0-1..7 verified with durable evidence; `PROJECT_SKILLS.md` matches disk;
guard self-test blocks protected commands; quality-gate `fast` returns PASS; no protected
action performed; evidence manifest hashes every artifact; council has no unresolved
STOP/CRITICAL/MAJOR.

## Rollback and recovery
All changes are additive and confined to the feature branch; rollback = delete the branch
(no schema/data/production change). `RUN_STATE.yaml` retains last-known-good. No
migrations executed.

## Prohibited actions
No merge to `main`; no production deploy; no secret disclosure; no provider/legal
acceptance; no billing change; no weakening of gates/permissions/hooks; no deletion of the
legacy spike or evidence.

## Exit gate and expected artifacts
Gate G-P0 (Governance ready). **PR base branch: `develop`** (NOT `main`) — required by
planning council DRM-1/RT-1 because the legacy `deploy.yml` auto-deploys to Railway on
push to `main` (see BLK-003). **STOP condition:** do not merge this branch to `main` while
`deploy.yml` is armed; main is human-merge-only. Expected artifacts: this WP doc; ADR-0001;
P0 traceability rows; `.gitattributes`; `scripts/make-evidence-manifest.py`;
`artifacts/evidence/P0/WP-P0-001/<run-id>/` with logs + `environment.json` + `manifest.json`
+ SHA256SUMS; `planning-council-findings.json`; `COUNCIL_REVIEW.md` + `council-findings.json`;
`DOCS/gates/GATE_G-P0.md` record; updated `RUN_STATE.yaml`, `WORK_QUEUE.yaml`, `DOCS/12_BLOCKERS.md`.

## Planning council outcome
Verdict APPROVE_WITH_CONDITIONS (quorum: Program Director + DevOps/Release + QA + Red-Team).
No STOP findings. CRITICAL/MAJOR remediated before implementation: PR routed to `develop`
(DRM-1/RT-1), evidence manifest generator added (QA-1/RT-2), `.gitattributes` LF (DRM-3),
`environment.json` (QA-2), registry-diff + guard self-test captured (QA-3); deploy/CI
reconciliation deferred to WP-P1-001 with compensating controls (DRM-2, BLK-003). See
`artifacts/evidence/P0/WP-P0-001/<run-id>/planning-council-findings.json`.
