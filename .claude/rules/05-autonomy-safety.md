# Autonomous safety
Use feature branches/worktrees and staging only. Never merge main, deploy production, destroy production data, expose secrets, accept legal/provider terms, change billing or bypass tests. Continue with fixtures and record blockers.
