# Authority and change control
The CDR and approved contracts control. Never silently change scope, data semantics, claims, gates or security/privacy boundaries. Create an ADR/change note and mark blocked if authority is missing.
